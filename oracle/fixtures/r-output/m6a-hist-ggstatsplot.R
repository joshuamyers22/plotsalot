list(subtitle_data = list(statistic = 0.916515138991168, p.value = 0.34, 
    n.obs = 7L, method = "Bootstrap-t method for one-sample test", 
    effectsize = "Trimmed mean", estimate = 4, conf.level = 0.95, 
    conf.low = 1.51294259305207, conf.high = 6.48705740694793, 
    expression = list(list(italic("t")["bootstrapped"] == "0.92", 
        italic(p) == "0.34", widehat(mu)["trimmed"] == "4.00", 
        CI["95%"] ~ "[" * "1.51", "6.49" * "]", italic("n")["obs"] == 
            "7"))), caption_data = NULL, pairwise_comparisons_data = NULL, 
    descriptive_data = NULL, one_sample_data = NULL, tidy_data = NULL, 
    glance_data = NULL)
